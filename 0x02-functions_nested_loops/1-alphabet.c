#include <studio.h>
/**
 * main - functions that print alphabet
 *
 * return: always 0.
 *
 */

void print_alphabet(void);
{
	int n=a;
	print_alphabet(void);
	while (n<z);
	{
		_putchar(print_alphabet [n]);
		n +=a;
	}
	_putchar('\n');
	return(0);
}
